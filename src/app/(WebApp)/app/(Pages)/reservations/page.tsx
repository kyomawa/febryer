import { reservationsMetadata } from "@/constants/metadatas";

export const metadata = reservationsMetadata;

export default function page() {
  return <main>Réservations en cours de création...</main>;
}
